Now I have the full picture. The scaffold is nearly complete — all Go source files are implemented, tests are written, the mandate and embed are in place. The two blockers are: `go.sum` is missing (project won't compile) and `internal/composer/` is an empty dead directory. Here is the precise spec.

---

# TASK-20260521-102217: Bootstrap teststop Build

**Feature**: `/Volumes/SATECHI_WD_BLACK_2/dev/teststop/teststop-init/04-AGENT-STARTING-PROMPT.md` — Start working on the instruction
**Type**: feature
**Status**: pending
**Created**: 2026-05-21T01:22:17Z

## Summary

The `teststop` Go CLI is structurally complete — all source files, tests, mandate, embed, and CLI wiring are implemented. The project cannot be built because `go.sum` is absent and `internal/composer/` is an empty directory that leaves a dangling package reference. The work is to make the project compile and all tests green.

## Files to Touch

| File | Action | Reason |
|------|--------|--------|
| `go.sum` | CREATE | Generated by `go mod tidy`; required for reproducible builds |
| `internal/composer/` | DELETE (dir) | Empty directory with no `.go` files; all mandate composition lives in `internal/mandate/composer.go` |

## Implementation Steps

### Step 1: Generate go.sum

**File**: `go.sum` (project root)
```
$ cd /Volumes/SATECHI_WD_BLACK_2/dev/teststop
$ go mod tidy
```
- Must be run with Go 1.22+ (matches `go.mod` directive)
- `go mod tidy` will download `github.com/spf13/cobra v1.8.1` and its indirect deps (`inconshreveable/mousetrap`, `spf13/pflag`) and write `go.sum`
- Do not edit `go.mod`; it is already correct

### Step 2: Remove the empty composer directory

**File**: `internal/composer/` (directory)
```
$ rm -rf /Volumes/SATECHI_WD_BLACK_2/dev/teststop/internal/composer
```
- The directory contains no `.go` files and is never imported
- All mandate composition is already implemented in `internal/mandate/composer.go`
- Leaving an empty directory does not break the build but is misleading scaffolding debris

### Step 3: Verify the build

**File**: all packages
```
$ go build ./...
```
- Expected: zero errors
- `mandate/embed.go` uses `//go:embed base.md`; `mandate/base.md` is present so embed will succeed
- `internal/ai/claude.go` and `openai.go` use raw `net/http` — no extra deps

### Step 4: Run all tests

**File**: all `*_test.go` files
```
$ go test ./...
```
- `internal/ai` — `adapter_test.go`: 4 tests (JSON parse, fence stripping, garbage rejection, single object)
- `internal/mandate` — `composer_test.go`: 3 tests (placeholder substitution, count scaling, maturity budget)
- `internal/memory` — `confidence_test.go` + `store_test.go`: 9 tests (nudge, decay, retire, revive, save/load roundtrip)
- `internal/reporter` — `reporter_test.go`: 4 tests (JSON roundtrip, text fields, Markdown sections, exit codes)
- All tests must pass; no test skips allowed

### Step 5: Smoke-test the dry-run path

**File**: `cmd/teststop/main.go` (runtime check)
```
$ go run ./cmd/teststop run --dry-run --path .
```
- Expected: prints the composed mandate to stdout with all `{{PLACEHOLDER}}` tokens substituted
- Confirms reader, detector, analyzer, composer, and embed all wire correctly end-to-end
- No AI key required for dry-run

## Acceptance Criteria

- [ ] `go.sum` exists and `go mod tidy` produces no changes on a second run
- [ ] `go build ./...` completes with zero errors or warnings
- [ ] `go test ./...` — all 20+ tests pass, zero failures, zero skips
- [ ] `teststop run --dry-run --path .` prints a resolved mandate with no `{{...}}` tokens remaining
- [ ] `internal/composer/` directory no longer exists
- [ ] Binary responds to `teststop --version` and `teststop mandate --show`

## Edge Cases

- `go mod tidy` on a machine with no internet: will fail to download deps — ensure network access or pre-warm the module cache
- `go:embed` at build time: if `mandate/base.md` is missing, build fails with `pattern base.md: no matching files found` — file is present, so this should not occur
- `go test` on the `internal/mandate` package: the test imports `internal/memory` and `internal/reader` transitively via the embedded `mandate.Base` string — confirm no import cycle before running
- Empty `go.sum` written by a corrupted `go mod tidy`: if checksum DB is unreachable, `go.sum` may be incomplete — use `GONOSUMCHECK=*` only as a last resort and document why

## Test Scenarios

| Scenario | Input | Expected |
|----------|-------|----------|
| Happy path build | `go build ./...` after `go mod tidy` | Zero exit code, binary produced |
| Full test suite | `go test ./...` | All packages pass |
| Dry-run smoke | `run --dry-run --path .` on teststop's own source | Mandate printed, zero `{{` tokens, exit 0 |
| `go mod tidy` idempotent | Run `go mod tidy` twice | `go.sum` unchanged on second run |
| Mandate show | `teststop mandate --show` | Prints `mandate/base.md` verbatim |
| Status on fresh project | `teststop status` | "No memory yet. Run `teststop run` to begin." |
| Error: no API key | `teststop run` without any key | Error: "no AI provider configured: set ANTHROPIC_API_KEY or OPENAI_API_KEY, or pass --provider" |

## Copilot Instructions Block

```
DEVLOOP TASK: TASK-20260521-102217
FEATURE: teststop — bootstrap project to compilable, all-tests-green state
TYPE: feature

IMPLEMENT: project bootstrap (go.sum generation + cleanup)
SIGNATURE: shell commands at /Volumes/SATECHI_WD_BLACK_2/dev/teststop

RULES:
  1. Run `go mod tidy` from the project root to generate go.sum — do not manually create or edit go.sum
  2. Delete the empty internal/composer/ directory — it contains no .go files and is dead scaffolding
  3. Run `go build ./...` and confirm zero errors before proceeding to tests
  4. Run `go test ./...` and confirm all packages pass — do not skip any test package
  5. Run `go run ./cmd/teststop run --dry-run --path .` and confirm no {{PLACEHOLDER}} tokens appear in output
  6. Do not modify any existing .go source files — the code is correct; only go.sum is missing
  7. Do not add, remove, or change any dependencies in go.mod

EDGE CASES:
  - no internet access: go mod tidy will fail — ensure module cache is warm or network is available
  - go:embed panic: if mandate/base.md were absent the embed would fail at build time — it is present, no action needed
  - import cycle check: internal/mandate imports internal/memory and internal/reader — verify no cycle with `go list -mod=mod ./...` if build fails unexpectedly

TESTS REQUIRED: yes
FRAMEWORK: go test (stdlib)
```
